#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_@angular_compiler-cli@11.0.9@@angular/compiler-cli/src/main.js" "$@"
  ret=$?
else 
  node  "$basedir/../_@angular_compiler-cli@11.0.9@@angular/compiler-cli/src/main.js" "$@"
  ret=$?
fi
exit $ret
