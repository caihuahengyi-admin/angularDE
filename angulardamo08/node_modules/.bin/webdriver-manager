#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_protractor@7.0.0@protractor/bin/webdriver-manager" "$@"
  ret=$?
else 
  node  "$basedir/../_protractor@7.0.0@protractor/bin/webdriver-manager" "$@"
  ret=$?
fi
exit $ret
