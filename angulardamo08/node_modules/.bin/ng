#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_@angular_cli@11.0.7@@angular/cli/bin/ng" "$@"
  ret=$?
else 
  node  "$basedir/../_@angular_cli@11.0.7@@angular/cli/bin/ng" "$@"
  ret=$?
fi
exit $ret
